# 环境变量示例（复制为 .env.local 后按需修改；.env.local 不会被提交）
# 见 dev_docs/01-sensitive-info.md

# 后端 API 代理目标地址（vite.config.js 的 /api 代理 target）
# 缺省回退到原公开 IP（http://8.138.106.241/api），生产环境请改为自有域名
VITE_API_TARGET=http://8.138.106.241/api
